# Public values only. Never add service/secret keys or SMTP passwords with VITE_.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SITE_URL=https://gcet-campus.vercel.app
# Enable only after Supabase Phone MFA + SMS provider are configured and tested.
VITE_PHONE_MFA_ENABLED=false
# Optional CAPTCHA site key only, if CAPTCHA is configured in Supabase Auth.
VITE_TURNSTILE_SITE_KEY=
# Optional analytics; leave unset for no third-party tracking.
VITE_GA_ID=
